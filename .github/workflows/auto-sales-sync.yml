name: Auto Sales Sync

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  scheduler-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Scheduler disabled
        run: |
          echo "Automatic Sales Sync scheduler is disabled."
          echo "Sales updates are performed manually from Admin Settings -> Update Sales -> RUN NOW."
