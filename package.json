{
  "name": "sales-monitoring-dashboard-online",
  "version": "1.0.0",
  "private": true,
  "description": "Role-based online sales monitoring dashboard with Excel upload, admin master data, independent section filters, charts, and exports.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node server.js",
    "check": "node --check server.js && node --check public/dashboard.js && node --check public/admin.js && node --check public/login.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.0",
    "xlsx": "^0.18.5"
  }
}
